<h1><%= t(:header) %> <%= @project.title %></h1>

<%= link_to t(:create_task), new_project_task_path %>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, project_task_path(id: task.id) %></li>
  <% end %>
</ul>
