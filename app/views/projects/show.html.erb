<h1><%= @project.title %></h1>
<p><%= @project.description %></p>

<%= link_to t(:tasks), project_tasks_path(project_id: @project.id) %>
<%= link_to t(:edit), edit_project_path %>
<%= link_to t(:delete), project_path,
            data: {
              turbo_method: :delete,
              turbo_confirm: t(:confirmation)
            } %>
